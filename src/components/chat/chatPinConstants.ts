/** Đồng bộ backend `MAX_PINNED_MESSAGES_PER_CONVERSATION`. */
export const MAX_PINNED_PER_CONVERSATION = 5;

/** Đồng bộ backend `MAX_PINNED_CHATS_TO_TOP` — ghim hội thoại lên đầu danh sách. */
export const MAX_PINNED_CHATS_TO_TOP = 5;
